# What this workflow does
# It waits for someone to open an Issue or open a Pull Request.
# It checks whether this is the person's first interaction with your repository.
# If it is their first Issue, it posts the issue-message.
# If it is their first Pull Request, it posts the pr-message.
# If the person has already opened an Issue or Pull Request before, the workflow does nothing.

Note: This workflow does not have a Run workflow button because it only runs when one of the specified GitHub events (issues or pull_request_target) occurs. If you want to be able to run it manually from the Actions tab, you can add workflow_dispatch: under the on: section.

# Name of the workflow shown in the GitHub Actions tab
name: Greetings

# Events that trigger this workflow
on:
  # Runs when a pull request is opened, reopened, or updated
  pull_request_target:

  # Runs when an issue is opened
  issues:

# Jobs to be executed
jobs:

  # Job name
  greeting:

    # Use the latest Ubuntu virtual machine provided by GitHub
    runs-on: ubuntu-latest

    # Permissions required by this workflow
    permissions:

      # Allows the workflow to post comments on issues
      issues: write

      # Allows the workflow to post comments on pull requests
      pull-requests: write

    # Steps to execute
    steps:

      # Use GitHub's First Interaction Action
      # This action detects a user's first issue or first pull request
      # and posts a welcome message.
      - uses: actions/first-interaction@v1

        # Inputs required by the action
        with:

          # Automatically generated GitHub token used to authenticate
          # and post comments on behalf of the workflow
          repo-token: ${{ secrets.GITHUB_TOKEN }}

          # Welcome message posted the first time someone creates an issue
          issue-message: "Message that will be displayed on users' first issue"

          # Welcome message posted the first time someone creates a pull request
          pr-message: "Message that will be displayed on users' first pull request"
